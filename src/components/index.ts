import * as styles from '@material-ui/styles';
import ThemeProvider from './shared/styles/ThemeProvider';
import fonts from './shared/fonts';

export * from '@material-ui/core';

export { ThemeProvider, styles, fonts };
