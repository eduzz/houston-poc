import { Meta, Typeset, Preview  } from '@storybook/addon-docs/blocks';
import { fonts, Typography } from '../../components';


<Meta title='Tokens/Typography' />

# Typography

 `import { Typography } from '@eduzz/houston';`

**Examples**
<Preview>
  <Typography variant='h1'>We are Houston call us! we solve problems</Typography>
  <Typography variant='h2'>We are Houston call us! we solve problems</Typography>
  <Typography variant='h3'>We are Houston call us! we solve problems</Typography>
  <Typography variant='h4'>We are Houston call us! we solve problems</Typography>
</Preview>

# Fonts

 `import { fonts } from '@eduzz/houston';`

 **Fonts props**
 <div>
  <table>
    {Object.keys(fonts).map(p =>
      <tr>
        <td>{p}</td>
        <td>fonts.{p}</td>
      </tr>
    )}
  </table>
</div>


**Font Family:**
<div>{fonts.fontFamily}</div>

**Weights:**
<div>
  <table>
    {Object.keys(fonts.weights).map(w =>
      <tr style={{ fontWeight: fonts.weights[w] }}>
        <td>{w}</td>
        <td>{fonts.weights[w]}</td>
        <td>fonts.weights.{w}</td>
      </tr>
    )}
  </table>
</div>

**Sizes:**
<div>
  <table>
    {Object.keys(fonts.sizes).map(s =>
    <tr>
      <td>{s}</td>
      <td>{fonts.sizes[s]}</td>
      <td>fonts.sizes.{s}</td>
      <td style={{ fontSize: fonts.sizes[s] }}>We are Houston call us! we solve problems</td>
    </tr>)}
  </table>
</div>

**Line Height:**
<div>
  <table>
    {Object.keys(fonts.lineHeight).map(l =>
    <tr>
      <td>{l}</td>
      <td>{fonts.lineHeight[l]}</td>
      <td>fonts.lineHeight.{l}</td>
      <td style={{ lineHeight: fonts.lineHeight[l] }}>We are Houston call us! we solve problems</td>
    </tr>)}
  </table>
</div>

**Spacings:**
<div>
  <table>
    {fonts.spacings.map(s =>
    <tr>
      <td>{s}</td>
      <td>fonts.spacings.{s}</td>
    </tr>)}
  </table>
</div>
